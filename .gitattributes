# Scripts run on Linux (and in WSL from a Windows checkout), so they must keep
# LF endings regardless of the client's core.autocrlf setting. A CRLF shebang
# line makes the kernel fail to find the interpreter.
*.sh text eol=lf
*.py text eol=lf
*.tsv text eol=lf
*.csv text eol=lf
