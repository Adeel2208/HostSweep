# Benchmark self-audit

Run directory: `C:\Users\HP\Documents\hotsweep\HostSweep\benchmark\run`


## 1. Row counts

  --   per_library.csv            ABSENT (expected 6 tools x 12 synthetic libraries x 3 runs = 216 (+ 30 real x 3 = 90 if the real arm ran))
  --   ablation.csv               ABSENT (expected 3 configs x 12 libraries x 3 runs = 108)
  --   threshold_sweep.csv        ABSENT (expected 5 entropies x 5 lengths x 3 libraries = 75)
  --   synthetic_manifest.csv     ABSENT (expected 12)
  --   accessions_verified.csv    ABSENT (expected 30)
  --   downstream.csv             ABSENT (expected 3 methods x (12 synthetic + 6 real) = 54)
  --   kraken2_human.csv          ABSENT (expected 3 methods x 18 libraries = 54)

## 2. Provenance: every row traceable to files on disk

  --   per_library.csv absent or empty; nothing to trace

## 3. Replicate spacing: measured or generated?

Real replicates of a deterministic pipeline are *identical*; real replicates of a timed one are *irregularly scattered*. Gaps that repeat across many groups are the signature of values generated by perturbing a single measurement.

  --   no replicate data to check

## 4. Cross-file agreement

  --   per_library.csv or ablation.csv absent; skipped
  --   per_library.csv or synthetic_manifest.csv absent; skipped

## 5. Carried-over values

  --   per_library.csv absent; nothing to compare

## Verdict

**No result file exists.** The benchmark produced no data, so there is nothing to audit and nothing to report. This is a complete result in itself: absent, not empty, not estimated.
