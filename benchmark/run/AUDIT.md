# Benchmark self-audit

> **This file is a self-audit from a later benchmark session**, run against
> that session's own local `out` directory, where its raw evidence (metrics
> JSON, `.time` files) actually lives — that is the only complete evidence
> tree available for that session. Running `audit.py` against this git
> checkout instead reports every row as lacking a metrics JSON, because raw
> evidence is deliberately kept outside the repo (see
> `benchmark/scripts/wsl_run.sh`); that is a structural mismatch, not a
> finding about the data.
>
> The `ABSENT` lines below (`ablation.csv`, `threshold_sweep.csv`,
> `accessions_verified.csv`) are **not gaps** — those three were produced
> earlier and are committed in this directory; this session's own `out/`
> never held them because it never ran E5, E7 or E1. Row counts for
> `per_library.csv`, `downstream.csv` and `kraken2_human.csv` are this
> session's own contribution only (36, 16, 16) — the merged, committed files
> in this directory carry more (72, 18, 18) once the earlier session's rows
> are included. See `RESULTS.md` section 1 for the current, combined status
> of every experiment, and `DEVIATIONS.md` D18 for what is and is not
> comparable between the two runs.

Run directory: `~/hostsweep/out`


## 1. Row counts

  --   per_library.csv            36 rows (expected 6 tools x 12 synthetic libraries x 3 runs = 216 (+ 30 real x 3 = 90 if the real arm ran))
  --   ablation.csv               ABSENT (expected 3 configs x 12 libraries x 3 runs = 108)
  --   threshold_sweep.csv        ABSENT (expected 5 entropies x 5 lengths x 3 libraries = 75)
  --   synthetic_manifest.csv     12 rows (expected 12)
  --   accessions_verified.csv    ABSENT (expected 30)
  --   downstream.csv             16 rows (expected 3 methods x (12 synthetic + 6 real) = 54)
  --   kraken2_human.csv          16 rows (expected 3 methods x 18 libraries = 54)

## 2. Provenance: every row traceable to files on disk

  OK   all 36 rows have a metrics JSON
  OK   all 36 rows have a .time file

## 3. Replicate spacing: measured or generated?

Real replicates of a deterministic pipeline are *identical*; real replicates of a timed one are *irregularly scattered*. Gaps that repeat across many groups are the signature of values generated by perturbing a single measurement.

  0 groups identical across replicates (expected for deterministic steps)
  0 groups vary across replicates

## 4. Cross-file agreement

  --   per_library.csv or ablation.csv absent; skipped
  OK   host_pct matches the manifest's realised fraction in all 36 rows

## 5. Carried-over values

  OK   no computed mean coincides with a pre-existing published figure

## Verdict

All checks passed on the files that exist.
